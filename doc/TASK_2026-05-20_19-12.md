# Task List - Restore Podcast Episode Deletion
**Created: 2026-05-20 19:12**

- [x] Exclude bidirectional fields from Lombok equals/hashCode generation
    - [x] Modify `PodcastChannel.java` (exclude `item` and `itunescategory`)
    - [x] Modify `PodcastItem.java` (exclude `chanel`)
    - [x] Modify `PodcastItunesCategory.java` (exclude `chanel`)
- [x] Refactor deletion service logic
    - [x] Import `Transactional` and annotate `DeleteEpisode` in `PodcastService.java`
    - [x] Safely handle parent-child dissociation inside `DeleteEpisode`
- [x] Simplify Controller layer deletion endpoint
    - [x] Modify `deleteEpisode` in `EpisodeEditController.java` to delegate entirely to the service
- [x] Compilation & Verification
    - [x] Run `gradlew compileJava` to ensure code compiles cleanly
    - [x] Verify that deletion works without `StackOverflowError` or database errors

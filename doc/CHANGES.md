# Історія змін проекту RFA

## [2026-04-21] Фінальна стабілізація видалення та виправлення синтаксису
- **Виправлено**: Помилка `reached end of file while parsing` у `PodcastEditController.java`.
- **Виправлено**: Помилки `cannot find symbol` для `@RequestParam` та `storeService` у контролерах подкастів.
- **Стабільність**: Проведено повну перевірку компіляції всього проекту.

---

## [2026-04-21] Розширене видалення контенту та стабілізація подкастів
- **Новий функціонал**: Впроваджено можливість видалення епізодів подкастів із вибором: видаляти лише запис у БД чи також фізично видалити аудіофайл із сервера.
- **Видалення подкастів**: Додано функцію повного видалення каналу подкасту разом з обкладинкою та епізодами.
- **Виправлено**: 
    - Проблема збереження обкладинок епізодів для нових подкастів.
    - Помилка `TemplateProcessingException` у `user_page.html`.

---
...

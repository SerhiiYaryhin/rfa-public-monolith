# Історія змін проекту RFA

## [2026-04-21] Виправлення помилок у шаблонах подкастів
- **Виправлено**: Помилка `TemplateProcessingException` у `podcast/view.html`. Назву поля `item.itunesduration` змінено на коректну `item.timetrack`.
- **Виправлено**: (Попередньо) Помилка відображення дати публікації епізодів (`item.pubDate`).

---

## [2026-04-21] Виправлення помилки іменування властивостей JPA
...

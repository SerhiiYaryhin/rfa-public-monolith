# Історія змін проекту RFA

## [2026-04-22] Виправлення помилки відображення розміру файлу
- **Виправлено**: Помилка `TemplateProcessingException` на сторінці `store/edititem.html`. Назву поля `contentLength` змінено на коректну `filelength` відповідно до моделі `Store.java`.
- **Стабільність**: Додано перевірку на `null` для поля розміру файлу, щоб запобігти помилкам при діленні.

---

## [2026-04-22] Оновлення дизайну редагування елементів сховища
...

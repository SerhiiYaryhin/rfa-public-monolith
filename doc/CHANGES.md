# Історія змін проекту RFA

## [2026-04-22] Сортування файлів у Сховищі (Newest First)
- **Покращено**: Змінено порядок відображення файлів у глобальному сховищі для адміністратора (`/admin/storage`). Тепер найновіші файли відображаються на початку списку завдяки сортуванню за `createdate` у зворотному порядку.
- **Бекенд**: Оновлено метод `GetStorePage` у `StoreService.java` з використанням `Sort.by("createdate").descending()`.

---

## [2026-04-22] Глобальне Сховище для Адміністратора
...

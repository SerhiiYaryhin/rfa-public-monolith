# Історія змін проекту RFA

## [2026-04-20] Розширення адмін-панелі: Модерація постів та поштова інтеграція
- **Новий функціонал**: Впроваджено систему повної модерації постів. Адміністратор тепер бачить повний список публікацій системи.
- **Бекенд**: 
    - В `AdminPosts.java` додано метод `togglePostPublish` для швидкої зміни статусу поста.
    - В `PostRepositore` та `PostService` реалізовано вибірку всіх постів з сортуванням за датою створення.
- **Фронтенд**: 
    - Оновлено фрагмент `listpostcontrol` у `admin.html`: додано інтерактивні перемикачі (Switch) та візуальне підсвічування неопублікованих постів.
    - **Поштова інтеграція**: Додано кнопку швидкого зв'язку з автором поста через універсальний інструмент розсилок.

---

## [2026-04-20] Розширення адмін-панелі: Редагування треків та стабілізація UI
- **Новий функціонал**: Додано можливість перегляду та редагування метаданих треків (автор, назва, стиль, опис) безпосередньо з панелі адміністратора.
- **Бекенд**: В `AdminTracks.java` реалізовано GET/POST методи для управління даними треку.
- **Фронтенд**: Створено шаблон `admin/edittrack.html` з інтегрованим TinyMCE та вбудованим аудіоплеєром для перевірки контенту.
- **Виправлено**: Критична помилка `TemplateProcessingException` у списку треків адміністратора, спричинена спробою заперечення `null` значення (поле `apruve`). Впроваджено безпечні перевірки `track.apruve == true`.
- **Покращено UI**: До списку треків додано кнопку швидкого переходу до редагування.

---

## [2026-04-20] Виправлення кодування та стабілізація Поштового офісу
- **Виправлено**: Проблема відображення кирилиці при завантаженні шаблонів листів. Додано явне кодування `charset=UTF-8` у відповіді контролера `AdminEmailController`.
- **Архітектура**: Підтверджено та закріплено використання поля `email` як основного логіна та ідентифікатора користувача.
- **Виправлено**: Критична помилка `TemplateProcessingException` у `admin/email_sender.html`.
- **Покращено**: Оптимізовано JavaScript-фільтрацію отримувачів за ролями. Використовується проекція списку ролей (`u.roles.![role.name()]`) для надійного пошуку в DOM.
- **Стабільність**: Усунуто потенційні проблеми при роботі з користувачами, у яких не заповнений профіль.

---

## [2026-04-19] Рефакторинг завантаження та система модерації треків

### Огляд
Було проведено комплексний рефакторинг процесу завантаження аудіофайлів та впроваджено повноцінну систему модерації. Створено універсальний інструмент "Поштовий офіс" для гнучкого спілкування з користувачами.

### Внесені зміни

#### 1. Бекенд (Java / Spring Boot)
*   **`AdminEmailController` (Новий)**: Хаб для керування розсилками. Доступний для ролей ROLE_ADMIN, ROLE_MODERATOR, ROLE_EDITOR.
*   **`EmailSenderService`**: Додано функціонал динамічного зчитування шаблонів та метод `sendRawHtmlEmail`.
*   **`AdminTracks` & `AdminService`**: Реалізовано модерацію, каскадне видалення та перемикання статусів публікації.
*   **`CreaterService`**: Оновлено для підтримки асинхронного завантаження через UUID.

#### 2. Фронтенд (Thymeleaf / JavaScript)
*   **`admin/email_sender.html` (Новий)**: Інтерфейс із вибором отримувачів та інтеграцією **TinyMCE**.
*   **`trackupload.html`**: Новий двоетапний інтерфейс завантаження треків.
*   **`admin/tracks.html`**: Панель керування медіатекою з перемикачами статусів.

#### 3. Безпека та Стабільність
*   **UUID**: Впроваджено ідентифікацію через `uuid` для публічних посилань.
*   **Null-Safety**: Додано захист від порожніх об'єктів у шаблонах.
*   **Конфігурація**: Налаштовано `mail.from` у всіх профайлах.

### Результат
Проект отримав завершений цикл управління медіа-контентом та потужний інструмент комунікації з користувачами.

# Історія змін проекту RFA

## [2026-04-22] Виправлення помилки видалення файлів (Foreign Key Constraint)
- **Виправлено**: Помилка `DataIntegrityViolationException` при видаленні файлів зі сховища, які використовуються в епізодах подкастів.
- **Рішення**: Реалізовано механізм "м'якого від'єднання" (`DetachStoreFromPodcasts`). Перед видаленням файлу система тепер автоматично зануляє посилання на нього в усіх епізодах та каналах подкастів.
- **Бекенд**:
    - Оновлено `EpisodeRepository` та `ChanelRepository`: додано пошук за об'єктами `Store`.
    - Додано логіку очищення в `PodcastService.java`.
    - Оновлено `CreaterStoreController.java` для виклику процедури очищення перед видаленням.

---

## [2026-04-22] Сортування файлів у Сховищі (Newest First)
...

# Історія змін проекту RFA

## [2026-04-23] Усунення NPE у фрагментах треків
- **Виправлено**: Помилка `TemplateProcessingException` на сторінці всіх треків. Додано перевірки на `null` для об'єктів `Store` у фрагментах `lasttrecks` та `lasttrecksall`.
- **Стабільність**: Забезпечено коректний рендеринг списків треків навіть при відсутності пов'язаних медіа-файлів.
- **UUID**: Проведено фінальну заміну `post.id` на `post.uuid` у фрагментах новин.

---

## [2026-04-22] Відновлення відображення контенту на головній сторінці
...
